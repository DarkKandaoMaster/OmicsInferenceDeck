InferenceDeck/frontend/
├── app/
│   ├── assets/                  # 静态资源
│   │   └── css/
│   │       └── tailwind.css            # Tailwind 基础指令引入
│   │
│   ├── components/                     # 页面组件（按功能区域划分）
│   │   ├── layout/                     
│   │   │   └── AppHeader.vue           # 顶部导航栏（Logo+路由菜单）
│   │   ├── analysis/               # 分析配置区域组件
│   │   │   ├── AnalysisContainer.vue   # 总容器
│   │   │   ├── DataUpload.vue          # 1. 数据上传模块（组学与临床数据）
│   │   │   ├── AlgorithmSelect.vue     # 2. 算法选择、算法参数配置模块（包含“我想测试自己的算法”、“开启测试模式”两个按钮，以及自定义算法结果数据上传模块）
│   │   │   └── AnalysisActions.vue     # 底部按钮
│   │   └── results/                # 结果展示区域组件
│   │       ├── ResultsContainer.vue    # 总容器
│   │       ├── EvaluationMetrics.vue   # 数学指标（重构时记得把它独立于所有图表之外）
│   │       ├── ClusteringChart.vue     # A. 聚类散点图 (散点图)
│   │       ├── DiffAnalysis.vue        # B. 差异表达分析 (火山图、热图)
│   │       ├── Enrichment.vue          # C. 功能富集分析 (条形图、气泡图)
│   │       ├── SurvivalCurve.vue       # D. 临床预后生存分析 (KM曲线)
│   │       └── ParameterSearch.vue     # 参数敏感性分析结果 (2D/3D图)
│   │
│   ├── composables/                    # 抽取全局状态与复用逻辑
│   │   ├── core/
│   │   │   ├── useSession.ts            # 会话管理：UUID 生成、beforeunload 的 beacon 清理逻辑
│   │   │   └── useUIState.ts            # 全局 UI 状态：控制全局 loading、侧边栏折叠等
│   │   ├── domain/                      # 按业务领域划分的状态与逻辑
│   │   │   ├── useDataConfig.ts         # 组学与临床数据状态：文件列表、格式校验、上传API调用
│   │   │   ├── useAlgorithmConfig.ts    # 算法参数状态：选中算法、K值、迭代次数、模式开关(测试/自定义)
│   │   │   ├── useClustering.ts         # 聚类主流程：Run Analysis请求、降维切换、保存基础特征结果
│   │   │   └── useDownstream.ts         # 下游分析：包含差异分析、富集分析(GO/KEGG)、生存分析的API请求
│   │   └── utils/
│   │       └── useEcharts.ts            # 图表管家：封装图表的 init、dispose、resize 和基础 option 设定
│   │
│   ├── layouts/                        
│   │   └── default.vue                 # 全局基础布局（包含 AppHeader，留出主要内容 <slot />）
│   │
│   ├── pages/                          # 基于文件系统的真实路由 (利用 Nuxt 文件路由替代 activeTab 变量)
│   │   ├── index.vue                   # 对应 "Home" 页面 (Hero 区域)
│   │   ├── analysis.vue                # 对应 "Analysis" 页面 (核心工作台)
│   │   ├── resources.vue               # 对应 "Resources" 页面
│   │   └── help.vue                    # 对应 "Help" 页面
│   │
│   └── app.vue                         # 极简的系统入口
│
├── public/                         # 静态资源 (如图标等)
│   ├── favicon.ico
│   └── robots.txt
├── nuxt.config.ts
├── tailwind.config.ts                  # Tailwind 配置文件
├── package.json
└── tsconfig.json